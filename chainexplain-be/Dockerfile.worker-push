FROM node:22-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY src/ ./src/

# Cloud Run injects PORT env var (default 8080)
ENV PORT=8080

CMD ["node", "src/worker/push-server.js"]
